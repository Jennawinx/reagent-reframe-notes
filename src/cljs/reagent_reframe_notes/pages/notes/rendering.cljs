(ns reagent-reframe-notes.pages.notes.rendering
  (:require
   [reagent.session :as session]))

;; NOTE: Keep entry point as the bottom most function
(defn rendering-page []
  [:div "Rendering"
   [:span.main
    [:h1 "Routing test"]]])



